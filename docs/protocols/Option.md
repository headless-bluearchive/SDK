# Option 协议

Option 模块相关协议。

字段结构仅作为 SDK DTO 与发包实现参考；实际可用性以真实网关返回为准。

## 协议列表

| 协议名 | 协议号 | 作用 | RequestClass | ResponseClass |
| --- | ---: | --- | --- | --- |
| `Option_Save` | `53000` | 设置：保存状态 | `OptionSaveRequest` | `OptionSaveResponse` |

## 字段结构参考

### Option_Save

- 协议号：`53000`
- 作用：设置：保存状态
- RequestClass：`OptionSaveRequest`
- ResponseClass：`OptionSaveResponse`
- 状态：待补结构。

#### Request 字段

无字段或未匹配到结构。

#### Response 字段

无字段或未匹配到结构。
